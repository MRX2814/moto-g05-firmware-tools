# ADB i Fastboot

`DRAFT` — komendy zostaną dopisane po weryfikacji na Moto G05.

```text
adb devices
adb reboot bootloader
fastboot devices
fastboot getvar product
```

Nie wykonuj komend kasujących bez upewnienia się, że wskazują właściwe urządzenie.
