# Firmware i flashowanie Fastboot

`DRAFT` — nie używaj tej instrukcji jako gotowej procedury, dopóki nie zostanie oznaczona jako przetestowana.

Pakiet roboczy: `fastboot_lamu_g_user_15_VVTAS35.51_153_3_76a43c_release_keys`.

Zawiera między innymi obrazy `boot.img`, `vendor_boot.img`, `super.img`, `vbmeta.img`, pliki BAT oraz `MT6768_Android_scatter.xml`. Przed publikacją trzeba potwierdzić wariant, policzyć SHA-256, rozdzielić pełne flashowanie od aktualizacji bez kasowania danych i sprawdzić redystrybucję.
